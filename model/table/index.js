let bins = require('./bins')
let products = require('./products')
let shopCards = require('./shopcards')
let slips = require('./slips')
let users = require('./users')

module.exports = {
    bins,
    products,
    shopCards,
    slips,
    users,
}